{
  "name": "deploy-assets",
  "version": "1.0.0",
  "description": "Utility to deploy frontend assets to cloud storage",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk": "^2.814.0",
    "dotenv": "^8.0.0",
    "gulp": "^4.0.2",
    "randomstring": "^1.1.5"
  }
}
